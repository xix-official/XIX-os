name: "CodeQL"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  analyze:
    permissions:
      security-events: write
      contents: read
    uses: github/codeql-action/analyze@v3
